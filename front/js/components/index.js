/**
 * `components/index.js` exists simply as a 'central export' for rendering components back to Rails.
 */

export { default as renderApp } from './App'
export { default as renderTime } from './Timestamp'